{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "data/transit_route.location.updated",
  "title": "TransitRouteLocationUpdated",
  "description": "Event payload for transit route location updates",
  "type": "object",
  "additionalProperties": false,
  "required": ["lat", "lon", "updated_at"],
  "properties": {
    "lat": {
      "type": "number",
      "minimum": -90,
      "maximum": 90,
      "description": "Latitude coordinate of the current location"
    },
    "lon": {
      "type": "number",
      "minimum": -180,
      "maximum": 180,
      "description": "Longitude coordinate of the current location"
    },
    "transit_route_item_index": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Index of the transit route item being updated"
    },
    "updated_at": {
      "type": "integer",
      "minimum": 0,
      "maximum": 4102462800,
      "description": "Unix timestamp of when the location was updated"
    }
  }
}
